TODO (features):
- compile message into multiple requests and audio hints instead of a single request
-- also fixes audio tag
-- also add break tag with no argument handling
- feature: direct hardware input to grab and release focus
- add proper icons to TableEdit

TODO (misc quality):
- handle regex format errors 
- handle tag content errors instead of instead of letting the daemon crash  
- min window sizes for all windows
- check tab order in all windows
- handle voice list not being loaded yet when settings is opened

TO TEST:
