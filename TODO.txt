TODO (features):
- reimplement autocomplete and tag on tab
- feature: user tags
- feature: smml audio tag usage
-- if only 1 audio tag exists: shortcut to loading the sound directly
- feature: direct hardware input to grab and release focus
- add proper icons to TableEdit

TODO (internal):
- refactor MessageLexer
-- MessageLexer: check for content inside tags instead of letting the daemon crash
- refactor autocomplete
-- consider changing autocomplete and tab to insert tag to handle (invalid) nested tags
-- consider changing autocomplete logic to handle typing in the middle of the text
-- handle text selection

TODO (misc quality):
- min window sizes for all windows
- check tab order in all windows
- document [wait {time}] format
- handle voice list not being loaded yet when settings is opened

TO TEST:
